global = 0;
{
	x = 1;
	assert(x == 1, "scope symbol invalid");
	assert(global == 0, "scope parent symbol invalid");
};
{
	x = 2;
	assert(x == 2, "scope symbol invalid");
};
{
	x = 3;
	assert(x == 3, "scope symbol invalid");
};
{
	x = 4;
	assert(x == 4, "scope symbol invalid");
};